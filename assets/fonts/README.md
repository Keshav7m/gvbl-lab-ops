IBM Plex Sans (SIL Open Font License 1.1) — embedded in generated PDFs so that
Unicode characters such as µ, ±, ° and en-dashes render correctly. If these files are removed,
the PDF generator falls back to the built-in Helvetica font.
